extends testgameplay-layout

block content
    #tetris
        canvas#canvas1.canvas
        #menu
            p#start
                a#title(href='javascript:play();') Press Space to Play.
                h1#joined Playing against myself
            p
                canvas#upcoming
            p
                | score
                span#score 00000
            p
                | rows
                span#rows 0
        script(src='/js/testGameplay.js')


